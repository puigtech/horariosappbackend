'use strict';

/**
 *  dates-available-by-agent controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::dates-available-by-agent.dates-available-by-agent');
