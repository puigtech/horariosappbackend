'use strict';

/**
 * dates-available-by-agent router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::dates-available-by-agent.dates-available-by-agent');
