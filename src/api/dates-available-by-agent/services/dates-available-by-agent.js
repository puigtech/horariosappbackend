'use strict';

/**
 * dates-available-by-agent service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dates-available-by-agent.dates-available-by-agent');
