'use strict';

/**
 *  schedules-day-leader controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schedules-day-leader.schedules-day-leader');
