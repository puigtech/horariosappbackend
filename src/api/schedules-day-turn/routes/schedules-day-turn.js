'use strict';

/**
 * schedules-day-turn router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schedules-day-turn.schedules-day-turn');
