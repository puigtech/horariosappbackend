'use strict';

/**
 * schedules-day-turn service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::schedules-day-turn.schedules-day-turn');
