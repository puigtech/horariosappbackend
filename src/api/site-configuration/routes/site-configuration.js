'use strict';

/**
 * site-configuration router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::site-configuration.site-configuration');
